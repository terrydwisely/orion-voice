[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "orion-voice"
version = "0.1.0"
description = "Orion Voice - Text-to-Speech and Speech-to-Text for Windows"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
